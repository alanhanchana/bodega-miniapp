import type { FC } from 'react';

export const IndexPage: FC = () => {
  return (
    <div style={{ padding: '2rem', fontSize: '20px', fontWeight: 'bold' }}>
      🚨 If you're seeing this, the Mini App is rendering correctly.
    </div>
  );
};
